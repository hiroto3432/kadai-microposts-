<% if current_user.favoriting?(micropost) %>
  <%= form_for(current_user.favorites.find_by(user_id: current_user.id, micropost_id: micropost.id), html: { method: :delete }) do |f| %>
    <%= hidden_field_tag :microposts_id, micropost.id %>
    <%= f.submit 'UnFav', class: 'btn btn-danger btn-xs' %>
  <% end %>
<% else %>
  <%= form_for(current_user.favorites.build) do |f| %>
    <%= hidden_field_tag :microposts_id, micropost.id %>
    <%= f.submit 'Fav', class: 'btn btn-success btn-xs' %>
  <% end %>
<% end %>